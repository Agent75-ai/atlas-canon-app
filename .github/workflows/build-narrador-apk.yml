name: Build Narrador Offline Neural APK

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build temporarily paused while updating Narrador 2.1 sources"
